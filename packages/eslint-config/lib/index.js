const baseESlint = require('./base');
const vue3ESlint = require('./vue3');
const reactESlint = require('./react');
const prettier = require('./prettier');

module.exports = {
  baseESlint,
  vue3ESlint,
  reactESlint,
  prettier,
};
